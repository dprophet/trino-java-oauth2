# Environment variables for running E2E tests from the host (not in Docker)
#
# Copy this file to .env.local and the Makefile targets will automatically use it:
#   cp .env.local.template .env.local
#
# The Makefile will automatically source .env.local before running E2E tests.

# Hydra URLs for host access (localhost instead of Docker hostname 'hydra')
export HYDRA_ADMIN_URL=http://localhost:4445
export HYDRA_PUBLIC_URL=http://localhost:4444
